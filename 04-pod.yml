## Annotations
apiVersion: v1
kind: Pod
metadata:
  name: nginx04
  annotations:
    xyz.com.ecommerce.roboshop/component: nginx
spec:
  containers:
    - name: nginx
      image: nginx

---

apiVersion: v1
kind: Pod
metadata:
  name: nginx04.01
  labels:
    app: nginx
spec:
  containers:
    - name: nginx01
      image: nginx01